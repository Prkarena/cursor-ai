# Support Boundaries

This repository is not a project scaffold. It provides AI behavior only.

## Non-Goals
- It does not generate build tooling or runtime configuration.
- It does not add frameworks or dependencies.
